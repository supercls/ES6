// export default function diag(x){
// 	return x*3
// }
export const sqrt = Math.sqrt;// 使用 export{接口} 导出接口， 大括号中的接口名字为上面定义的变量， import和export是对应的；
export var s1=function(){
	return 8
}
// export default function shw(x){
// 	return x*7   如果一个js模块文件就只有一个功能， 那么就可以使用export default导出；
// }